package com.internship.tool.service;

import org.springframework.stereotype.Service;

/**
 * Dummy IncidentService (disabled real logic)
 */
@Service
public class IncidentService {
    // empty service to avoid dependency errors
}